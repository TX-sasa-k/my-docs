# Yaml構文規則（日本語訳）.md 差分レポート

## 概要

本レポートは、`Yaml構文規則（日本語訳）.md` の **main ブランチ版** と **Claude4.8 ブランチ版** を比較した結果をまとめたものです。

> **メモ**
> ご依頼に基づき、ファイル冒頭の見出し（H1／導入セクション見出し）の違いは比較対象から除外しています。
> 両ファイルは同一の GitHub Actions ワークフロー構文リファレンス（日本語訳）であり、扱っているキーワードとセクション構成はほぼ一致しています。差異は主に **各セクション本文の翻訳・記述の詳細レベル** に集中しています。

| 項目 | 内容 |
| --- | --- |
| 比較対象ファイル | `Yaml構文規則（日本語訳）.md` |
| main ブランチ コミット | `8384269e519a5298b90cb18d990ab1d2f7f7275e`（PR #13 GPT5.5 由来） |
| Claude4.8 ブランチ コミット | `84287694718bf1ff5c0176d60bf44a5785b96f1a`（PR #14 Claude4.8 由来） |
| 総行数（概算） | main: 約 2,970 行 / Claude4.8: 約 2,969 行 |
| セクション構成 | ほぼ同一（`name` 〜「フィルターパターンのチートシート」まで） |

---

## 1. 冒頭（見出し・導入）の違い ※参考情報

> 見出しは比較対象外ですが、両者の識別のため参考として記載します。

| 箇所 | main ブランチ | Claude4.8 ブランチ |
| --- | --- | --- |
| H1 見出し | `# GitHub Actions ワークフロー構文（日本語訳）` | `# YAML ワークフロー構文について` |
| 導入セクション見出し | `## ワークフローの YAML 構文について` | （導入セクション見出しなし。本文が直接続く） |

導入本文（「ワークフローファイルは YAML 構文を使用し…」以降）は両ブランチで実質同一です。

---

## 2. 本文（見出し以外）の主な差異

見出しを除いた本文レベルでは、両ファイルは **ほぼ同一の内容** です。以下に確認された相違点を記載します。

### 2-1. 訳語・表記の揺れ

同じ原文に対する訳語の選択に、以下のような揺れが見られます。

| 用語（英語原文） | main ブランチの訳 | Claude4.8 ブランチの訳 |
| --- | --- | --- |
| override | 「上書きする」 | 「オーバーライドする」（`jobs.<job_id>.env` / `defaults` 系セクションで顕著） |
| default（設定値） | 「デフォルト」「既定」が混在 | 「既定」「デフォルト」が混在 |

> **メモ**
> これらは意味的に等価な訳語の違いであり、記載されている技術的内容そのものに差はありません。
> セクションによっては main 側が「上書き」、Claude4.8 側が「オーバーライド」を用いるなど、翻訳担当（生成モデル）の違いに起因する表記の揺れです。

### 2-2. 記述の詳細度・言い回しの違い

`concurrency`、`permissions`、`jobs.<job_id>.strategy` などの長文セクションでは、
文単位の区切り方や補足の言い回しに軽微な差異があります。ただし、
説明している仕様・パラメーター・値の範囲（例: `queue: max` は最大 100、マトリックスは最大 256 ジョブ 等）は
両ブランチで完全に一致しています。

### 2-3. コード例（YAML サンプル）

`run-name`、`on`、`permissions`、`jobs`、`strategy.matrix`、`services`、
「フィルターパターンのチートシート」などに含まれる YAML / テキストのコード例は、
**両ブランチで内容が一致** しています（インデント・キー・値ともに同一）。

---

## 3. セクション構成の一致状況

以下の主要セクションは、見出し・出現順ともに両ブランチで一致しています。

- `name` / `run-name` / `on`
- `on.<event_name>.types`
- `on.<pull_request|pull_request_target>.<branches|branches-ignore>`
- `on.push.<branches|tags|branches-ignore|tags-ignore>`
- `on.<push|pull_request|pull_request_target>.<paths|paths-ignore>`
- `on.schedule` / `on.workflow_call` 系 / `on.workflow_run` 系 / `on.workflow_dispatch` 系
- `permissions`
- `env` / `defaults` / `defaults.run` / `defaults.run.shell` / `defaults.run.working-directory`
- `concurrency`
- `jobs` / `jobs.<job_id>` 配下の各キー
  （`name` / `permissions` / `needs` / `if` / `runs-on` / `snapshot` / `environment` /
  `concurrency` / `outputs` / `env` / `defaults` / `steps` 系 / `strategy` 系 /
  `container` 系 / `services` 系 / `uses` / `with` / `secrets` 系）
- フィルターパターンのチートシート

---

## 4. 結論

- **見出しを除く本文の内容は両ブランチで実質的に同一** です。
- 検出された差異は、以下の 2 種類に限られます。
  1. 訳語の揺れ（例: 「上書き」⇔「オーバーライド」）
  2. 文の区切り方・言い回しの軽微な差
- 技術仕様、パラメーター、値の範囲、コード例に **意味的な相違はありません**。
- したがって、いずれのブランチの版も **リファレンスとしての内容的な等価性** を保っています。

> **メモ**
> より厳密な行単位（word-level）の差分が必要な場合は、
> ローカルで両ブランチをチェックアウトのうえ `git diff main:Yaml構文規則（日本語訳）.md Claude4.8:Yaml構文規則（日本語訳）.md`
> を実行することで、全差分を機械的に取得できます。
